title: "SPlagueNet"
description: Documentation for SPlagueNet project
theme: just-the-docs
search_enabled: true
color_scheme: dark

url: "https://loch3f.github.io"
baseurl: "/PPS-25-SPlagueNet"

defaults:
  - scope:
      path: ""
    values:
      layout: default

mermaid:
  # Version of mermaid library
  # Pick an available version from https://cdn.jsdelivr.net/npm/mermaid/
  version: "9.1.3"